<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Dalamud.NET.Sdk/15.0.0">

  <PropertyGroup>
    <Version>1.1.0.0</Version>
    <PackageProjectUrl>https://github.com/link-0402/AdvancedPenumbraItemConverter</PackageProjectUrl>
    <PackageLicenseExpression>AGPL-3.0-or-later</PackageLicenseExpression>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <AssemblyName>AdvancedPenumbraItemConverter</AssemblyName>
    <RootNamespace>AdvancedPenumbraItemConverter</RootNamespace>
    <Configurations>Debug;Release</Configurations>
    <Platforms>x64</Platforms>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)'=='Debug'">
    <DefineConstants>DEBUG</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <!-- Keep the plugin deployable as a single entry assembly. The same sources are
         also built by the standalone Core project for offline tests. -->
    <Compile Remove="AdvancedPenumbraItemConverter.Core\**\*.cs" />
    <Compile Include="AdvancedPenumbraItemConverter.Core\*.cs" />
    <Compile Remove="AdvancedPenumbraItemConverter.Tests\**\*.cs" />
    <Compile Remove="TestData\**\*.cs" />
  </ItemGroup>
</Project>
